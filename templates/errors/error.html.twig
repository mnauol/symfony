<!DOCTYPE html>
<head>
    <meta charset="UTF-8">
    <title>Error {{ error_code }}</title>
</head>
<body>
    <h1>Error {{ error_code }}</h1>
    <p>{{ message }}</p>
    <a href="{{ path('home') }}">Home</a>
</body>
</html>